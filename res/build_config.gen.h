#pragma once

#define PROJECT_VERSION "${PROJECT_VERSION}"
#define PROJECT_NAME    "${PROJECT_NAME}"

// TODO: Git commit hash
// TODO: Support for release candidates
// TODO: Derive version from git tag:  git describe --tag
